import "reflect-metadata";

describe("reaction", () => {
  describe("add reaction", () => {
    // new reaction
    // invalid type
    // not auth

    test("new reaction", async () => {});
  });

  describe("change reaction", () => {
    // switch
    // invalid type
    // different author
    // not auth
  });

  describe("remove reaction", () => {
    // existing
    // not existing
    // whole post
    // different author
    // not auth
  });
});
