subscription UpdateReactions($postId: Float!) {
  updateReactions(postId: $postId) {
    id
    type
    postId
    commentId
    author {
      id
      firstName
      lastName
    }
    createdAt
    updatedAt
  }
}
