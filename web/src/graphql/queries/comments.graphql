query Comments($where: CommentWhereInput) {
  comments(where: $where) {
    id
    postId
    replyId
    body
    author {
      firstName
      lastName
      image_url
    }
    createdAt
    updatedAt
  }
}
