query User($userId: Float!) {
  user(userId: $userId) {
    id
    firstName
    lastName
    email
    image_url
    posts {
      id
    }
    comments {
      id
    }
    reactions {
      id
    }
    createdAt
    updatedAt
  }
}
